# Client (Formspree)
# Replace with your Formspree form endpoint
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/SEU_ID

# Backend server (optional if using Nodemailer)
PORT=4000

# SMTP configuration
# Example for Gmail (requires App Password):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=seuemail@gmail.com
# SMTP_PASS=app_password_aqui

# Example for Mailtrap (sandbox)
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_SECURE=false
# SMTP_USER=xxxxxx
# SMTP_PASS=yyyyyy

# Where to send messages (defaults to SMTP_USER if not set)
MAIL_TO=kevindevweb@gmail.com
# From header (defaults to "Portfolio Site" <SMTP_USER>)
MAIL_FROM="Portfolio Site" <kevindevweb@gmail.com>
